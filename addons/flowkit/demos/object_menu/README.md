# Object Mode menu → game

1. Open `object_menu.tscn`
2. Run (F5)
3. Press **Start Game** → loads `object_only` demo

Start button uses recipe **Start → Object-Only Demo** (`ui_button` pack).

Optional: open the button in Inspector → Object Mode → set another `scene_path`.
