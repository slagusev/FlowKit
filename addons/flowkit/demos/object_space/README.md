# Space ship demo (Object Mode)

RigidBody2D thrust + typewriter hint + camera follow.

Open `object_space.tscn` and run. Recipes auto-apply if missing.

Controls: **Up** thrust · **Left/Right** turn.
